name: CI

on:
  push:
    branches: [ main, dev, audit/tests-refactor ]
  pull_request:
    branches: [ main ]

jobs:
  ruff:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3
    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.10'
        cache: 'pip'
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install ruff
        if [ -f requirements.txt ]; then pip install -r requirements.txt; fi
    - name: Lint with ruff
      run: |
        ruff check .

  pytest:
    runs-on: ubuntu-latest
    needs: ruff
    steps:
    - uses: actions/checkout@v3
    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.10'
        cache: 'pip'
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        if [ -f requirements.txt ]; then pip install -r requirements.txt; fi
        pip install pytest pytest-cov
        # Install the package in development mode to ensure imports work correctly
        pip install -e .
    - name: Test with pytest
      run: |
        # Run with PYTHONPATH set to current directory to ensure all modules are found
        PYTHONPATH=$PYTHONPATH:$(pwd) pytest -q -m "not slow"

  coverage-report:
    runs-on: ubuntu-latest
    needs: pytest
    steps:
    - uses: actions/checkout@v3
    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.10'
        cache: 'pip'
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        if [ -f requirements.txt ]; then pip install -r requirements.txt; fi
        pip install pytest pytest-cov
        # Install the package in development mode to ensure imports work correctly
        pip install -e .
    - name: Generate coverage report
      run: |
        # Run with PYTHONPATH set to current directory to ensure all modules are found
        PYTHONPATH=$PYTHONPATH:$(pwd) pytest -m "not slow" --cov=algorithms,problems,utils --cov-config=.coveragerc --cov-report=xml --cov-fail-under=65
    - name: Upload coverage report
      uses: actions/upload-artifact@v4
      with:
        name: coverage-report
        path: coverage.xml
